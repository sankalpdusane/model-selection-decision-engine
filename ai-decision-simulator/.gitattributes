# Normalize line endings to LF on check-in (Windows CRLF → LF in repo)
* text=auto eol=lf

# Force binary for images — never mess with their bytes
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.svg text eol=lf
